<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="004" author="nat">
        <createTable tableName="revoked_tokens">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_revoked_tokens_user" references="users(id)" deleteCascade="true"/>
            </column>
            <column name="token_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="expiry_date" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Индексы для быстрого поиска и очистки устаревших токенов -->
        <createIndex tableName="revoked_tokens" indexName="idx_revoked_tokens_token_hash">
            <column name="token_hash"/>
        </createIndex>

        <createIndex tableName="revoked_tokens" indexName="idx_revoked_tokens_expiry_date">
            <column name="expiry_date"/>
        </createIndex>

        <createIndex tableName="revoked_tokens" indexName="idx_revoked_tokens_user_id">
            <column name="user_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
